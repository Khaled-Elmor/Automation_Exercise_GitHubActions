{
  "name": "pr_new",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": { 
    "clean:reports": "npx rimraf allure-report allure-results && npx mkdirp allure-report allure-result",
    "generate:reports": "npx allure generate ./allure-results --clean",
    "pretest": "npm run clean:reports", 
    "posttest": "npm run generate:reports", 
    "api": "npx playwright test API",
    "ETEflows": "npx playwright test tests/UserFlow",
    "safari": "npx playwright test --project=webkit ",
    "web": "npx playwright test example --grep @web ", 
    "script:to:run": "num run api && npm run ETEflows",
    "test": "npm run script:to:run || npm run posttest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.1",
    "allure-playwright": "^3.4.2"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
